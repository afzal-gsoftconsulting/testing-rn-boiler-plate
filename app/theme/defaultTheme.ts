import {DefaultTheme} from 'react-native-paper';

export const defaultTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: 'tomato',
    secondary: 'yellow',
    heading: 'red',
  },
};
